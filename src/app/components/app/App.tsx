import React from 'react';
import styles from './app.module.scss';

const App: React.FC = () => {
  return (
    <div className={styles['app-layout-container']}>
    </div>
  );
}

export default App;
